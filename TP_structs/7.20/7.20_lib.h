#include <stdio.h>

struct Complex
{
       float real;
       float imag;
};

struct Complex Suma(struct Complex ,struct Complex  );
struct Complex Resta(struct Complex ,struct Complex );
struct Complex Multiplicacion(struct Complex ,struct Complex );
struct Complex Conjugado(struct Complex );
struct Complex Division(struct Complex ,struct Complex );
