#include <stdio.h>
#include <stdlib.h>
#define N 4

unsigned char suma_saturada(unsigned char, unsigned char);
unsigned char *suma_saturada_vector(unsigned char *, unsigned char *, unsigned int);